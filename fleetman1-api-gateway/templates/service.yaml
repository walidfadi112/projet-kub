apiVersion: v1
kind: Service
metadata:
  name: {{ include "fleetman1-api-gateway.fullname" . }}
  labels:
    {{- include "fleetman1-api-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fleetman1-api-gateway.selectorLabels" . | nindent 4 }}
